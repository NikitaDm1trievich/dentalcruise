---
/**
 * Группа позиций прайса: подзаголовок и строки «название — цена».
 * Строка со старой ценой показывает её зачёркнутой рядом с новой.
 */
interface Props {
  title?: string;
  items: {
    title: string;
    price: string;
    was?: string;
    note?: string;
    href?: string;
    promo?: boolean;
  }[];
}

const { title, items } = Astro.props;
---

<div class="dc-price">
  {title && <h3 class="dc-price__title">{title}</h3>}
  <ul class="dc-price__list" role="list">
    {
      items.map((item) => (
        <li class="dc-price__row">
          <span class="dc-price__name">
            {item.href ? (
              <a href={item.href} class="dc-price__link">
                {item.title}
              </a>
            ) : (
              item.title
            )}
            {item.note && <span class="dc-price__note">{item.note}</span>}
          </span>
          <span class="dc-price__values">
            {(item.promo || item.was) && <span class="dc-price__promo">Акция</span>}
            {item.was && <s class="dc-price__was">{item.was}</s>}
            <b class="dc-price__value">{item.price}</b>
          </span>
        </li>
      ))
    }
  </ul>
</div>

<style>
  /* Таблица живёт и в узкой колонке прайса (три в ряд), и во всю ширину на
     странице услуги. Раскладку строки решаем по ширине самой таблицы, а не
     экрана: на странице прайса колонка узкая даже на большом мониторе. */
  .dc-price {
    display: grid;
    gap: var(--space-4);
    container-type: inline-size;
  }
  .dc-price__title {
    font-size: var(--type-h4);
    color: var(--text-strong);
  }
  .dc-price__list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
  }
  /* В узкой колонке название и цена идут друг под другом: раньше строка была
     переносимым flex-контейнером из трёх частей, и длинное название
     раскидывало метку и цену по разным местам — соседние строки вставали
     вразнобой. Теперь порядок один и тот же в любой строке. */
  .dc-price__row {
    display: grid;
    gap: 6px var(--space-4);
    padding: 12px 0;
    border-bottom: 1px solid var(--line-hairline);
  }
  @container (min-width: 420px) {
    .dc-price__row {
      grid-template-columns: minmax(0, 1fr) auto;
      align-items: baseline;
    }
    .dc-price__values {
      justify-content: flex-end;
    }
  }
  .dc-price__row:last-child {
    border-bottom: 0;
  }
  .dc-price__name {
    display: grid;
    gap: 2px;
    min-width: 0;
    color: var(--text-body);
  }
  .dc-price__link {
    color: inherit;
    text-decoration: none;
    /* Подчёркивание-опознователь ссылки: тонкая линия снизу, а не сплошной underline */
    text-decoration: underline;
    text-decoration-color: var(--line-strong);
    text-underline-offset: 3px;
    transition: color var(--dur-fast) var(--ease-out), text-decoration-color var(--dur-fast) var(--ease-out);
  }
  .dc-price__link:hover {
    color: var(--text-accent);
    text-decoration-color: var(--text-accent);
  }
  .dc-price__note {
    font-size: var(--type-small);
    color: var(--text-muted);
  }
  /* Метка идёт первой в блоке цен и всегда рядом с ними: так она читается
     как пометка к цене, а не как отдельный элемент, гуляющий по строке. */
  .dc-price__promo {
    align-self: center;
    flex: 0 0 auto;
    padding: 3px 10px;
    border-radius: var(--radius-pill);
    background: var(--dc-coral);
    color: var(--text-on-coral);
    font-family: var(--font-body);
    font-size: var(--type-eyebrow);
    font-weight: var(--fw-bold);
    letter-spacing: var(--ls-eyebrow);
    white-space: nowrap;
  }
  .dc-price__values {
    display: flex;
    flex-wrap: wrap;
    align-items: baseline;
    gap: var(--space-2) var(--space-3);
  }
  .dc-price__was {
    color: var(--text-muted);
    font-size: var(--type-small);
    text-decoration-thickness: 1px;
  }
  .dc-price__value {
    font-family: var(--font-display);
    font-weight: var(--fw-black);
    color: var(--text-accent);
    white-space: nowrap;
  }
</style>
