# Адрес Cloudflare Worker, который принимает заявку с формы записи
# и пересылает её в Telegram-бота. Публичная переменная: попадает
# в клиентский бандл, поэтому в ней только URL — никаких токенов.
PUBLIC_BOOKING_ENDPOINT="https://dentalcruise-booking.<ваш-субдомен>.workers.dev/booking"
